This is ${project.name} version ${project.version}
Description: ${project.description}
Developed by: ${project.organization.name}
Website: ${project.organization.url}

To install the ${project.name} service, you need to execute bin/install.cmd as administrator.
This script installs the service.

To uninstall the ${project.name} service, you need to execute bin/uninstall.cmd as administrator.